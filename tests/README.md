# Tests

Root test documentation lives here.

Current backend unit tests remain in:

```text
backend/tests/
```

Future cross-service and end-to-end tests should be added under this root `tests/` directory.
